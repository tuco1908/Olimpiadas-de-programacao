#include <iostream> 
#include <stack>

using namespace std;

int main(){

    int n, d;

    while(cin >> n >> d && n != 0 && d != 0){
        char exp[n];
        int count;
        stack<int> q;

        cin >> exp;

        d = n - d;

        //444499
        
        while( x > q.top() && ){

        }
        
        
        
        
        //n - count >= (d - q.size() + 1)
            //while para deletar
        }

        /*
        1 - pilha cheia, mas o número é maior que o último
            remove números menores que este até que tenha
            espaço suficiente para os números restantes
        
        */


    }

}